import 'package:flutter/material.dart';

class DetailScreenLoadingState extends StatelessWidget {
  const DetailScreenLoadingState({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const CircularProgressIndicator();
  }
}
