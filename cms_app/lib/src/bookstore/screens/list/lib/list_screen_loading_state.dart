import 'package:flutter/material.dart';

class ListScreenLoadingState extends StatelessWidget {
  const ListScreenLoadingState({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const CircularProgressIndicator();
  }
}
